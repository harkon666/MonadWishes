# Foundry Build Outputs
out/
cache/
broadcast/

# Environment Variables & Private Keys
.env
.env.local
*.env
lib/

# Dependencies (if installed via git submodules or npm)
# Note: Keep lib/ if storing submodules, but ignore untracked artifacts inside lib
# lib/

# Node / System files
node_modules/
.DS_Store
*.log
